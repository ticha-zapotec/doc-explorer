# ticha-doc-explorer

## Getting Started

### Prerequisites
- Git
- Node and NPM (recommended via asdf)

## Installation
1. Clone the repository and `cd` into it
2. Run `npm install`

## Local Development
1. Run `npm run dev`